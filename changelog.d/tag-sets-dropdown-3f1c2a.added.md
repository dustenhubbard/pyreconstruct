- **Tag sets: standard tag choices in Set Attributes.**
  A series can carry named lists of tags. A pick-one list gets its own
  dropdown, and the Tags rows complete from the pick-many lists and show
  each tag's definition as a tooltip.
