- **The status readout stays on the left and holds still.** It keeps its
  familiar left-side home instead of moving to the right edge, and its width
  no longer follows the cursor coordinates, so it does not jitter under the
  pointer. While a notice is up, the notice briefly takes its place, then the
  readout returns.
