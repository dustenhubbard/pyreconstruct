- **Smoothing has a keyboard shortcut.** Ctrl+Shift+R smooths the selected
  traces on this section; over the object list the same key smooths the
  selected objects whole. Remappable like any other shortcut.
